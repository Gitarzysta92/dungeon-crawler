import { Injectable } from "@angular/core";
import { SuggestionService } from "../../game/services/suggestion.service";

@Injectable()
export class AdventureSuggestionService extends SuggestionService {
  hideAllSuggestions() {

  } 

    // allowedActivities.forEach(a => {
  //   a.isHighlighted = true;
  //   a.subject.isHighlighted = true
  // });

  // allowedActivities.forEach(a => {
  //   a.isHighlighted = false;
  //   a.subject.isHighlighted = false
  // });

}